apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.squareup:javapoet:1.8.0'
    compile project(':piri-annotation')
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
